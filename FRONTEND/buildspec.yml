version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:s
    - '**/*'
  discard-paths: no
  base-directory: build
